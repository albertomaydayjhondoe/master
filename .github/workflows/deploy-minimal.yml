name: 🚀 Minimal Deploy - Stakas MVP

on:
  push:
    branches: [ main, Meta ]
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to Railway
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v4
      
    - name: 🚂 Deploy to Railway
      uses: railwayapp/railway-deploy@v3
      with:
        railway-token: ${{ secrets.RAILWAY_TOKEN || 'dummy-token' }}
        service: stakas-mvp-${{ github.ref_name }}
        
    - name: ✅ Success
      run: |
        echo "🚀 Deployed ${{ github.ref_name }} successfully!"
        echo "📺 Canal: UCgohgqLVu1QPdfa64Vkrgeg"