# Model configuration for production use
# Note: Train models specific to your target social media platform

yolo_screenshot:
  # Path to trained YOLOv8 weights (Ultralytics)
  model_path: /app/data/models/production/social_ui_detector.pt
  
  # Inference device (cuda/cpu)
  device: cuda
  
  # Detection thresholds
  conf_threshold: 0.25
  iou_threshold: 0.45

  # Training configuration
  training:
    data_yaml: /app/data/datasets/social_ui/data.yaml
    epochs: 100
    imgsz: 640
    batch: 16
    
yolo_video:
  model_path: /app/data/models/production/social_video_analyzer.pt
  device: cuda
  
affinity:
  model_path: /app/data/models/production/account_affinity.onnx
  device: cuda
  
anomaly:
  model_path: /app/data/models/production/anomaly_detector.pt
  device: cuda